# JavaScript project

Uses esbuild for building, and jest for unit test.

To setup JavaScript dependencies locally:

```bash
npm install
```

To lint program:

```bash
npm run lint
```

To run program:

```bash
npm run app
```

To run unit tests:

```bash
npm run test
```
